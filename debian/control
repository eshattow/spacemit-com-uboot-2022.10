Source: u-boot-spacemit
Section: admin
Priority: optional
Maintainer: weizhi.li <weizhi.li@spacemit.com>
Build-Depends: bc,
 bison,
 debhelper-compat (= 13),
 flex,
 libpython3-dev:native [linux-any],
 libssl-dev,
 python3:any [linux-any],
 python3-pyelftools [linux-any],
 python3-setuptools [linux-any],
 swig [linux-any],
 device-tree-compiler,
 hart-payload-generator [riscv64],
 libgnutls28-dev,
 libncurses-dev,
 libncurses-dev:native,
 libssl-dev:native,
 lzop:native,
 uuid-dev,
Rules-Requires-Root: no
Standards-Version: 4.6.2
Homepage: https://www.denx.de/wiki/U-Boot/
Vcs-Browser: https://github.com/spacemit-com/uboot-2022.10
Vcs-Git: https://github.com/spacemit-com/uboot-2022.10.git
XBS-Commit-Id: ${misc:CommitId}

Package: u-boot-spl-spacemit
Architecture: riscv64
Multi-Arch: foreign
Depends: ${misc:Depends},
 kmod,
Recommends: opensbi-spacemit
Breaks: u-boot-spacemit (<< ${binary:Version})
Replaces: u-boot-spacemit (<< ${binary:Version})
Description: SPL (FSBL) bootloader for SpacemiT systems
 Das U-Boot is a cross-platform bootloader for embedded systems,
 used as the default boot loader by several board vendors.  It is
 intended to be easy to port and to debug, and runs on many
 supported architectures, including PPC, ARM, MIPS, x86, m68k,
 NIOS, and Microblaze.
 .
 This package contains the First Stage Boot Loader (FSBL) for the
 SpacemiT K3 platform: the U-Boot SPL wrapped with the SpacemiT
 image header, the bootinfo blobs and the default U-Boot
 environment. These are written to the boot media during package
 installation.

Package: u-boot-spacemit
Architecture: riscv64
Multi-Arch: foreign
Depends: ${misc:Depends},
 kmod,
 u-boot-spl-spacemit,
Conflicts: edk2-spacemit
Built-Using: ${u-boot-spacemit:Built-Using}
Description: U-Boot proper for SpacemiT systems
 Das U-Boot is a cross-platform bootloader for embedded systems,
 used as the default boot loader by several board vendors.  It is
 intended to be easy to port and to debug, and runs on many
 supported architectures, including PPC, ARM, MIPS, x86, m68k,
 NIOS, and Microblaze.
 .
 This package contains U-Boot proper for the SpacemiT K3 platform,
 packaged as a FIT image (u-boot.itb) bundling U-Boot and the
 device trees. It depends on u-boot-spl-spacemit for the First Stage
 Boot Loader chain; only the u-boot.itb payload is written to the
 boot media by its postinst. It conflicts with edk2-spacemit, which
 boots the same platform through UEFI instead of U-Boot proper.
